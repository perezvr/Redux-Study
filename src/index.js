import React from 'react';
import { render } from 'react-dom';

import App from './App';

/**
 * Essa função é responsável por renderizar um componente do react em algum arquivo HTML
 */
render(<App />, document.getElementById('root'));
